import { ScrollAnimDirective } from './scroll-anim.directive';

describe('ScrollAnimDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollAnimDirective();
    expect(directive).toBeTruthy();
  });
});
